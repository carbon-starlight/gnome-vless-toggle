# VLESS Toggle GNOME Shell Extension

Quickly activate VLESS proxy with sing-box from Quick Settings panel

Uses configuration file named `config.json` saved in the following path: `~/.config/sing-box/`
